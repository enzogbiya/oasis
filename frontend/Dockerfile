# Установка базового образа
FROM node:16.20.0

# Установка рабочей директории в контейнере
WORKDIR /

# Копирование зависимостей и файла package.json в контейнер
COPY package.json .

# Установка зависимостей
RUN npm install

# Копирование остальных файлов проекта в контейнер
COPY . .

# Сборка приложения React
RUN npm run build

# Открытие порта, если ваше приложение слушает определенный порт
EXPOSE 3000

# Команда для запуска приложения в контейнере
CMD [ "npm", "start" ]
